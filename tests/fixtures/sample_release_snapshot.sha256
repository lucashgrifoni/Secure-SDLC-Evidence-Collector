# Structural SHA-256 of examples/sample_release/bundle.json after stripping
# volatile fields (bundle_id, generated_at, evidence[*].collected_at,
# control_evaluations[*].evaluated_at) and POSIX-normalizing
# evidence[*].raw.artifact_path. See
# tests/integration/test_bundle_snapshot.py and
# src/evidence_collector/application/integrity.py.
#
# Regeneration workflow (only when an intentional bundle change lands):
#
#   1. pytest tests/integration/test_bundle_snapshot.py — the test prints
#      the new digest in its failure output.
#   2. Replace the digest line below.
#   3. Explain in the PR description why the bundle changed and link the
#      change in CHANGELOG.md.
#
# 2026-05-17 (cross-OS fix, intermediate): bumped from 34f3025e… to
# a42920b3… after fixing cross-OS determinism in normalize_bundle
# (Windows runs produced a different digest because
# evidence[*].raw.artifact_path contained '\\' separators).
#
# 2026-05-18: bumped from a42920b3… to 5e1498cd… in the same hardening
# branch. The bundle.json content did not change; the input fixtures
# under examples/sample_release/artifacts/ were renormalized from CRLF
# to LF by the mixed-line-ending pre-commit hook on 2026-05-17, which
# changed their SHA-256 (carried into bundle.json as raw.integrity_hash).
# Cross-OS stability re-confirmed on Windows and Linux-simulated runs.
#
# 2026-05-18 (Tier 5 — EPSS/KEV enrichment scaffolding): bumped from
# 5e1498cd… to 7d8b94c7… after the schema gained two new evidence-level
# fields (``cve_ids``, ``vulnerability_intelligence``). The new fields
# default to an empty list / null respectively so the change is fully
# backwards compatible, but their presence alters the canonical JSON
# representation and therefore the structural hash.
#
# 2026-06-22 (C2 — CISA 2025 SBOM conformance): bumped from 7d8b94c7… to
# 788ac044… after SBOM evidence gained two metadata fields
# (``cisa_2025_minimum_elements`` map + ``cisa_2025_conformant`` bool), a
# content-derived presence check against CISA's 2025 Minimum Elements.
# Additive and byte-stable per run; only the canonical representation (and
# therefore the structural hash) changed.
# 2026-08-06 (3.0.0 — human-readable rationales): bumped from 788ac044… to
# 8c0b0a9b… after control rationales stopped rendering Python's list repr.
# `Control SSDF-PW.7 is met by evidence ['sast-cfcf7a634b32'].` became
# `Control SSDF-PW.7 is met by evidence sast-cfcf7a634b32.` across all 13
# rationales of the canonical bundle. The change is cosmetic in intent but it
# alters `control_evaluations[*].rationale`, which is inside the structural
# hash and inside the in-toto predicate — so it is a BREAKING change for anyone
# pinning `verify --expected` against the default profile, and is released as
# 3.0.0 with migration notes rather than folded into a patch.
#
# 2026-08-06 (3.0.0, same release — fixture URL repair): bumped again to
# 0171b09e… after examples/sample_release/artifacts/*.sarif and
# zap-baseline.json had `http./sample_release` replaced with real URLs. The
# corruption dated to v1.0.0 and leaked into the showcase report.md. Repairing
# the fixtures changes their SHA-256, which the bundle records as
# evidence[*].raw.integrity_hash — hence the second bump. Both bumps ship in
# 3.0.0 deliberately, so consumers absorb one digest change rather than two.
#
#
# 2026-08-07 (3.0.0, same release - bundle contract version): bumped from
# 6c420913... to 17fb007e... after BUNDLE_SCHEMA_VERSION moved 1.0.0 -> 2.0.0.
# `bundle_version` is inside the structural hash, so the digest moves with it.
#
# The version had to move. The bundle gained `collection_errors`, and the
# published schema is `additionalProperties: false` at every level, so a
# consumer validating against the 1.0.0 contract they pinned rejects any
# bundle carrying it - by this contract's own rules that is breaking, and the
# field that exists to announce it had stayed at 1.0.0 while the shape changed
# underneath. In the same change, serialization now omits `collection_errors`
# when empty, so a clean bundle still validates against 1.0.0 and only bundles
# that genuinely report a failed input carry the new shape.
#
# Note for whoever regenerates this next: .gitattributes sets `* text=auto
# eol=lf`, so the fixtures are stored and hashed as LF. Rewriting them from a
# Windows checkout with a text-mode write produces CRLF, whose SHA-256 differs
# — the digest computed locally then disagrees with CI. Write fixture bytes
# with LF explicitly, or read the digest off the CI failure. This is the same
# trap that caused the 2026-05-18 bump above.
#
# 2026-08-07 (3.0.0, same release - JUnit summary precision): bumped from
# 17fb007e... to 97273d00... after the test-result summary stopped reporting
# reported-vs-executed as one number. `42 tests executed, ... 1 skipped`
# became `42 tests reported, 41 executed, ... 1 skipped`, which is what the
# file actually says - 42 were not executed, 41 were.
#
# The reword rides along with a substantive fix in the same normalizer: a
# suite where nothing ran (tests="0", or every test skipped) used to be
# recorded as `passed` at HIGH confidence and satisfied the test-result
# control outright. A test job whose glob matched nothing now yields
# `invalid`, so the control comes out MISSING with the file still visible to
# the auditor. The canonical sample has 41 executed tests, so its status is
# unchanged; only the summary text moved the digest.
97273d00c233fd100dadf93d595640b50b3b0c6a88f86e9f605991e4a1f6e9eb
